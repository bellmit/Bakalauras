// export default let authorsFormattedForDropdown(authors)
// {
// 	if (!authors) {
// 		return
// 	}
//
// 	return authors.map((author) => ({
// 		value: author.id,
// 		text: `${author.firstName} ${author.lastName}`,
// 	}))
// }
